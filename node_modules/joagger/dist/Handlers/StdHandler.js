"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StdHandler = void 0;
var Looger_1 = require("../Looger");
var StdHandler = (function () {
    function StdHandler() {
    }
    StdHandler.prototype.log = function (details) {
        console.log(details.datetime.toISOString(), this.showLevel(details.level), details.channel.toUpperCase(), "|", details.message);
    };
    StdHandler.prototype.showLevel = function (level) {
        var Reset = "\x1b[0m";
        var FgRed = "\x1b[31m";
        var FgGreen = "\x1b[32m";
        var FgYellow = "\x1b[33m";
        if (level.wight < Looger_1.LogLevels.NOTICE.wight) {
            return FgGreen + level.name + Reset;
        }
        else if (level.wight <= Looger_1.LogLevels.WARNING.wight) {
            return FgYellow + level.name + Reset;
        }
        else if (level.wight === Looger_1.LogLevels.ERROR.wight) {
            return FgRed + level.name + Reset;
        }
        else {
            return FgRed + "!!!" + level.name + "!!!" + Reset;
        }
    };
    return StdHandler;
}());
exports.StdHandler = StdHandler;
